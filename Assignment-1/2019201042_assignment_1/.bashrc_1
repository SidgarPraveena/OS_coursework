
#unset HISTFILESIZE
#HISTSIZE=3000
#PROMPT_COMMAND="history -a"
#export HISTSIZE PROMPT_COMMAND

#shopt -s histappend

alias ll='ls -alF'
alias mn='man'



if (( $EUID  ))
then
	PS1="@"
else
	PS1="#"
fi

